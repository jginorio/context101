__pycache__/
*.pyc
.venv/
data/
*.db
*.db-wal
*.db-shm
.env
oauth-tokens.txt
*.pem
*.key
*.zip

# CDK
cdk/node_modules/
cdk/cdk.out/
cdk/*.js
cdk/*.d.ts
cdk/lambda/**/node_modules/
backups/
.claude/
.agents/
skills-lock.json
.migration-inventory/
# impeccable skill live-mode session scratch
.impeccable/

# Monorepo workspace (anchored so the CDK Lambda-layer node_modules stays tracked)
/node_modules/
/packages/*/node_modules/
/packages/cli/stack/
# per-app lockfiles regenerate on install/build; only the root lock is canonical
web/package-lock.json

# Verification-skill run artifacts (proof stays on disk for the run; do not commit)
.cursor/skills/verify-context101/artifacts/

# Web app
web/node_modules/
web/.next/
web/out/
web/next-env.d.ts
web/*.tsbuildinfo

node_modules/